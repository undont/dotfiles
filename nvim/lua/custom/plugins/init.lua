-- plugin specs are auto-discovered by lazy.nvim's `{ import = 'custom.plugins' }`
-- (see init.lua). every *.lua file in this directory that returns a spec table is
-- loaded automatically; there is no manual registration list to maintain
return {}
