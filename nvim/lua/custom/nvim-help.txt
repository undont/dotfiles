╔══════════════════════════════════════════════════════════════════════════╗
║                          NVIM KEYBOARD SHORTCUTS                         ║
╚══════════════════════════════════════════════════════════════════════════╝

┌─ NAVIGATION ─────────────────────────┬─ SEARCH (<leader> = Space) ────────┐
│                                      │                                    │
│  hjkl                 move cursor    │  <leader>sf        find files      │
│  w / b               word fwd/back   │  <leader>sg        live grep       │
│  0 / $               line start/end  │  <leader>/         buffer search   │
│  gg / G              file top/bottom │  <leader><leader>  open buffers    │
│  Ctrl+d / Ctrl+u     half-page       │  <leader>sR        search/replace  │
│  Ctrl+o / Ctrl+i     jump back/fwd   │  <leader>?         full cheatsheet │
│  s                   flash jump      │                                    │
│  S                   flash treesitter│                                    │
│                                      │                                    │
└──────────────────────────────────────┴────────────────────────────────────┘

┌─ EDITING ────────────────────────────┬─ LSP ──────────────────────────────┐
│                                      │                                    │
│  i / a               insert at/after │  grd                go to def      │
│  o / O               new line below  │  grr                references     │
│  dd / yy             delete/yank     │  grn                rename         │
│  p / P               paste after/bef │  gra                code action    │
│  u / Ctrl+r          undo / redo     │  K                  hover docs     │
│  .                   repeat last     │  <leader>lr         restart LSP    │
│  <leader>f           format buffer   │  <leader>th         inlay hints    │
│  <leader>a / A       swap param next │                                    │
│                                      │                                    │
└──────────────────────────────────────┴────────────────────────────────────┘

┌─ TOOLS ──────────────────────────────┬─ DIFF / PR ────────────────────────┐
│                                      │                                    │
│  <leader>e           file tree       │  <leader>do         diff open      │
│  -                   oil (parent dir)│  <leader>dc         diff close     │
│  <leader>g           git (lazygit)   │  <leader>dh         file history   │
│  <leader>q           build project   │  <leader>dp         PR diff review │
│  <leader>xx          all diagnostics │  <leader>pl         list PRs       │
│  Ctrl+hjkl           move windows    │  <leader>pr         start review   │
│                                      │                                    │
├─ GIT HUNKS ──────────────────────────┼─ WINDOW RESIZE ────────────────────┤
│                                      │                                    │
│  <leader>hs          stage hunk      │  <leader>wh         resize left    │
│  <leader>hr          reset hunk      │  <leader>wl         resize right   │
│  <leader>hp          preview hunk    │  <leader>wj         resize down    │
│  <leader>hb          blame line      │  <leader>wk         resize up      │
│  <leader>hd          diff index      │                                    │
│                                      │                                    │
└──────────────────────────────────────┴────────────────────────────────────┘

                          Press q or Esc to close this help
