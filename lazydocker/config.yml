# LazyDocker configuration
# Managed by dotfiles — changes here will be overwritten on update

gui:
  returnImmediately: true
  theme:
    # Use reverse video for selections — works with any terminal colour scheme
    # and avoids poor contrast from ANSI colours used as backgrounds
    selectedLineBgColor:
      - reverse
    selectedRangeBgColor:
      - reverse

commandTemplates:
  # Show timestamps in the main (m) log views
  serviceLogs: '{{ .DockerCompose }} logs --since=60m --follow --timestamps {{ .Service.Name }}'
  viewServiceLogs: '{{ .DockerCompose }} logs --follow --timestamps {{ .Service.Name }}'
  allLogs: '{{ .DockerCompose }} logs --tail=300 --follow --timestamps'
  viewAllLogs: '{{ .DockerCompose }} logs --timestamps'

logs:
  timestamps: false
  since: '60m'
